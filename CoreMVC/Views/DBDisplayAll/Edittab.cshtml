@model CoreMVC.Models.Employee
@using (Html.BeginForm("Edittabn", "DBDisplayAll", new { id = Model.eid }, FormMethod.Post))
{
    @Html.HiddenFor(model=>model.eid)
    <div class="form-group">
        <label class="control-label">Employee Name</label>
        <input asp-for="ename" class="form-control"/>
    </div>
    <div class="form-group">
        <label class="control-label">Employee Address</label>
        <input asp-for="eaddr" class="form-control" />
    </div>
        <div class="form-group">
            <label class="control-label">Employee Salary</label>
            <input asp-for="esal" class="form-control" />
        </div>
        <div class="form-group">
            <input type="submit" value="Update" class="btn btn-success"/>
        </div>
}
<div>
    @Html.ActionLink("Back to List","AllProfile_Pageload","DisplayAll")
</div>